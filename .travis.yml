os: osx
osx_image: xcode11.1
cache: bundler
branches:
  only:
    - master

env:
  global:
  - LC_CTYPE=en_US.UTF-8
  - LANG=en_US.UTF-8
  - WORKSPACE=PulsarKit.xcworkspace
  - IOS_FRAMEWORK_SCHEME="PulsarKit"
  matrix:
    include:
    - osx_image: xcode11
      env: DESTINATION="OS=13.1,name=iPhone Pro"      SCHEME="$IOS_FRAMEWORK_SCHEME"
    - osx_image: xcode10
      env: DESTINATION="OS=12.1,name=iPhone XS"       SCHEME="$IOS_FRAMEWORK_SCHEME"
    - osx_image: xcode10
      end: DESTINATION="OS=11.4,name=iPhone X"        SCHEME="$IOS_FRAMEWORK_SCHEME"

before_install:
  - echo "BEFORE INSTALL START"
  - gem install bundler
  - echo "BEFORE INSTALL END"
  
script:
  - set -o pipefail
  - xcodebuild -workspace "$WORKSPACE" -scheme "$SCHEME" -destination "$DESTINATION" -configuration Debug ONLY_ACTIVE_ARCH=NO ENABLE_TESTABILITY=YES test | xcpretty
